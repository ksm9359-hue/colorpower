# 니 똥'컬러파워 - 구현 현황 상세 정리 (v0.1)

## 1) 프로젝트 개요

- 프로젝트명: `니 똥'컬러파워`
- 장르: 캐주얼 색상 매칭 회피/수집 게임
- 엔진/런타임: `Phaser` + `Vite` + `TypeScript`
- 플레이 목표: 낙하물을 받아서 같은 색 3개를 매칭해 점수를 쌓고, 부착 한계를 넘기지 않기

## 2) 현재 구현된 핵심 게임플레이

### 2-1. 플레이어 조작

- 키보드:
  - `A / D` 좌우 이동
  - `← / →` 좌우 이동 지원
- 모바일:
  - 화면 터치 후 좌/우 슬라이드(드래그) 방향으로 이동
  - 손을 떼면 이동 정지
- 게임오버 후 `SPACE`로 즉시 재시작 가능

### 2-2. 낙하물 시스템

- 낙하물 색상 타입: 5종 (`R/B/G/Y/P` 대응)
- 스폰 방식:
  - 시간 경과에 따라 스폰 간격이 점진적으로 감소
  - 시간 경과에 따라 한 번에 생성되는 개수(웨이브 수)가 증가
- 낙하 속도:
  - 플레이 시간이 늘어날수록 점진적으로 빨라짐
- 비주얼:
  - 단순 사각형이 아닌 똥 모양 2D 스프라이트(색상별 텍스처)로 생성됨

### 2-3. 충돌/부착/매칭 규칙

- 낙하물이 플레이어와 충돌하면 플레이어에 "부착"됨
- 부착 시:
  - 점수 즉시 증가
  - 색상별 부착 카운트 증가
  - 플레이어 위에 부착된 오브젝트가 시각적으로 유지됨
- 같은 색 3개 부착 시:
  - 해당 색상 3개 제거
  - 보너스 점수 지급
  - 매칭 성공 이펙트 출력
- 전체 부착 수가 5 이상이면 게임오버

## 3) 점수/밸런스 구현 상태

> 밸런스 수치 조정, 프리셋, 계산식, 테스트 로그 기준은 `balance-tool.md`를 단일 기준 문서로 사용한다.
> 본 문서에는 "현재 게임에 반영된 상태"만 요약한다.

### 3-1. 점수 규칙

- 기본 획득 점수: `+10`
- 3매칭 보너스: `+50`
- 난이도 보정 보너스:
  - 동시 생성 수가 높아질수록 추가 점수 보너스 적용
  - 고난도 구간에서 리스크/리턴이 커지도록 설계

### 3-2. 난이도 상승

- 스폰 간격 단축 + 낙하 속도 증가 + 동시 생성 수 증가를 조합
- 시간대가 길어질수록 실수 시 급격히 불리해지는 구조
- 의도:
  - 초반은 학습 구간
  - 중반부터 선택 압박 강화
  - 후반은 고득점/즉사 리스크가 공존

## 4) UI/피드백 구현 상태

- 상단 UI:
  - `Score`
  - `Attached: 현재부착/5`
  - 색상별 부착 현황 (`R/B/G/Y/P`)
- 매칭 성공 시:
  - 컬러 플래시 확산 이펙트
  - 파티클 분산
  - `+50 BONUS!` 부유 텍스트 표시
- 게임오버 시:
  - 최종 점수 표시
  - 재시작 안내 텍스트 표시

## 5) 기술/배포 구현 상태

### 5-1. 로컬 개발

- 개발 서버: `npm run dev`
- 빌드 확인: `npm run build` 성공 상태 유지

### 5-2. 외부 공유

- Netlify 배포 설정 완료 (`netlify.toml`)
- 운영 링크 사용 가능:
  - `https://poetic-daffodil-26fd95.netlify.app`
- 임시 터널(localtunnel) 대비 안정적인 모바일 접속 환경 확보

## 6) 파일 기준 변경 요약

- 주요 게임 로직: `src/main.ts`
  - 입력(키보드/모바일), 스폰, 충돌, 점수, 이펙트, 게임오버 처리
- 밸런스 원본 수치: `balance-config.json`
  - 런타임에서 import되어 실제 플레이에 반영
- 밸런스 관리 문서: `balance-tool.md`
  - 수치 기준값, 계산식, 프리셋, 플레이테스트 로그 관리
- 배포 설정: `netlify.toml`
- 기획 이력 문서: `plans.md`

## 7) 다음 개선 후보 (우선순위 제안)

1. `v0.2` 라운드 시스템 도입 (라운드별 고정 난이도 + 라운드 전환 상승 구조)
2. `v0.2` 퍽 선택 페이즈 도입 (라운드 종료 시 2지 선택, 세션 누적)
3. `v0.2` 폭탄 낙하물 타입 추가 (전 화면 클리어 + 제거 수 기반 보너스 점수)
4. 모바일 조작감 미세 튜닝 (슬라이드 민감도, 데드존, 손맛 고도화)
5. SFX를 파일 기반으로 교체 (`public/audio/sfx`) 및 믹싱 정교화
6. 랭킹 보안 강화 (입력 검증 고도화, 서버 정책 강화)
