# 게임 기획 정리 (최종 v0.1)

## 1) 게임 컨셉

- 장르: 캐주얼 피하기(회피) 게임
- 핵심 재미: 단순 회피가 아니라, 낙하물의 컬러를 보고 `피할지/충돌할지` 판단하는 플레이
- 목표: 같은 컬러를 매칭해 점수를 높이는 게임성

## 2) 조작 규칙

- `A / D` 키로 플레이어 캐릭터를 좌우 이동

## 3) 오브젝트

- 낙하물:
  - 총 5가지 컬러 타입
  - 랜덤으로 생성되어 위에서 아래로 떨어짐

## 4) 핵심 규칙

- 낙하물이 플레이어 캐릭터에 닿으면 플레이어에 "부착"됨
- 전체 부착 수가 5개 이상이면 게임오버
- 같은 컬러 낙하물이 3개 부착되면 해당 컬러 부착분 제거 + 보너스 점수 획득

## 5) 점수 규칙 (확정)

> 실제 적용 수치(오프셋 기준)는 `balance-tool.md`를 단일 기준으로 사용한다.

- 기본 점수: 낙하물 1개 처리 시 `+10`
- 보너스 점수: 같은 컬러 3매칭 시 `+50`

## 6) 플레이어 규칙 (확정)

- 플레이어 컬러: 회색 고정

## 7) 난이도 규칙 (확정)

> 증가량/구간/상한/하한 등 수치 상세는 `balance-tool.md` 기준으로 관리한다.

- 게임이 진행될수록 난이도가 점진적으로 서서히 상승
- 기준 방향: 시간 경과에 따라 낙하 속도/생성 빈도 완만 증가

## 8) 다음 확정 필요 항목

- 난이도 상승 수치(증가 주기, 최대 속도/최소 생성 간격)
- 점수 반영 기준(충돌 시 즉시 +10인지, 생존 시간 점수 별도 부여할지)
- UI 정보 구성(현재 점수, 부착 수, 컬러별 부착 현황 표시 방식)
- 사운드/이펙트 최소 범위

## 9) 현재 구현 반영 사항 (2026-04-30)

- 5색 낙하물 랜덤 생성 + 점진 난이도 상승(생성 간격 감소, 낙하 속도 증가)
- 충돌 시 부착 처리 및 부착 수/색상별 카운트 UI 표시
- 동일 색상 3개 매칭 시 제거 + `+50 BONUS!` 이펙트/텍스트 표시
- 총 부착 수 5개 이상 시 게임오버, `SPACE`로 재시작 가능
- 플레이어 색상 회색 고정, 조작은 `A / D`

## 10) 배포 공유 목표

- GitHub Pages 링크로 외부 플레이 테스트 공유
- 목표 링크 형식: `https://<github-id>.github.io/<repo-name>/`

## 11) 랭킹 시스템/씬 구조 기획 (신규)

### 11-1. 목표

- 하이스코어 경쟁 구조를 통해 재플레이 동기 강화
- 1회 플레이 결과가 즉시 랭킹에 반영되어 성과가 보이도록 설계

### 11-2. 랭킹 시스템 규칙 (v0.1)

- 랭킹 범위: 상위 `1~10위`
- 기록 단위: `플레이어 이름 + 점수`
- 정렬 기준: `점수 내림차순`
- 동점 처리: `먼저 기록된 플레이어 우선`
- 이름 규칙:
  - 최소 1자, 최대 10자
  - 공백-only 입력 불가
  - 금칙어/특수문자 필터는 추후 확정

### 11-3. 씬 구성 (v0.1)

#### A) 준비 화면 (로비)

- 표시 정보:
  - 게임 타이틀: `내 똥 칼라파워!`
  - 랭킹 UI: `순위 / 플레이어 이름 / 점수`
- 입력/행동:
  - 플레이어 이름 입력
  - 게임 시작 버튼
  - 인포 아이콘 탭 시 설명 팝업
- 인포 팝업 문구:
  - `같은 컬러를 3개 모아 최고 점수를 달성하세요.`

#### B) 인게임 화면

- 표시 정보 최소화:
  - 현재 점수
  - 랭킹 1위 점수(목표 지표)
- 비표시 항목:
  - 게임 타이틀
  - 부착 수/세부 상태 텍스트(추후 필요 시 재검토)

#### C) 종료 화면

- 표시 정보:
  - 최종 점수
  - 랭킹 `1~10위`
  - 현재 플레이어의 랭킹/점수/진입 여부
- 행동:
  - 확인 버튼 탭 시 준비 화면으로 이동
  - 플레이어 이름 입력값은 준비 화면으로 유지 전달

### 11-4. 화면 전환 플로우

- 준비 화면 -> 시작 버튼 -> 인게임
- 인게임 -> 게임오버 -> 종료 화면
- 종료 화면 -> 확인 버튼 -> 준비 화면

### 11-5. v0.1에서 제외된 항목 (다음 버전 검토)

- 랭킹 저장 범위: `로컬 디바이스` vs `서버 연동`
- 동점 처리 규칙 최종안
- 이름 유효성 규칙(허용 문자, 최대 길이, 금칙어)
- 인게임에서 부착 수를 완전히 제거할지 여부

## 12) v0.2 기획 초안 (라운드/퍽/신규 낙하물)

### 12-1. 목표

- 시간 기반 단일 루프에서 `라운드 기반 전략 루프`로 확장
- 플레이 실력 + 라운드별 퍽 선택 판단으로 하이스코어 메타 형성
- 반복 플레이 시 매 판 다른 빌드 감각 제공

### 12-2. 라운드 시스템

- 라운드는 `1`부터 시작하며, 종료될 때마다 `+1`
- 라운드 진행 구조:
  - `전투 페이즈`: 낙하물 생성/충돌/점수 획득 진행
  - `퍽 선택 페이즈`: 낙하물 생성 정지, 2지선다 선택 UI 표시
- 난이도 상승 규칙:
  - 같은 라운드 내에서는 `낙하물 속도/생성 수/생성 간격` 고정
  - 다음 라운드 시작 시 난이도 수치가 단계적으로 상승
- 라운드 종료 트리거(확정):
  - 시간 기반 종료 (`roundDurationSec` 경과 시 라운드 종료)
  - 라운드 HUD에 `Round N`과 함께 `다음 라운드까지 남은 시간`을 표시
  - 긴 플레이 타임에서 체감 지루함을 줄이기 위해 진행 시간을 명확히 피드백

### 12-3. 퍽 시스템

- 라운드 종료 시 2개 퍽 선택지 제공(랜덤 2지)
- 선택한 퍽은 해당 플레이 세션 동안 누적 유지
- 게임오버 시 퍽 효과 초기화
- 퍽 제시 규칙(확정):
  - 같은 선택 화면(2지) 내 동일 퍽 중복 제시 금지
  - 스택 가능한 퍽은 재등장 가능
  - 스택이 최대 중첩에 도달한 퍽은 더 이상 제시하지 않음
- 설계 의도:
  - 단순 반사신경 게임이 아니라 빌드 선택의 전략성을 부여
  - 중반 이후 운용 방식(리스크 관리/점수 극대화) 분기 유도

### 12-4. 퍽 풀 (v0.2 후보)

1. `플레이어 캐릭터 확장`
   - 플레이어 폭 증가
   - 부착 가능 수 `+1`
2. `플레이어 이동 속도 증가`
   - 좌우 이동 속도 고정값 상향
3. `다음 라운드 한정 컬러 타입 -1`
   - 다음 라운드에서 낙하물 컬러 종류 감소
4. `특정 컬러 블록 점수 보너스`
   - 지정 컬러 충돌/매칭 점수 추가
5. `낙하물 생성 속도 감소`
   - 생성 빈도 완화(간격 증가)
6. `낙하물 낙하 속도 감소`
   - 낙하 이동 속도 완화
7. `해당 라운드 폭탄 낙하물 확률 등장`
   - 폭탄 등장 확률 플래그 활성화

### 12-5. 신규 낙하물 타입: 폭탄

- 폭탄도 컬러 타입을 가짐
- 동일 컬러 매칭에 폭탄이 포함되어 3매칭이 성사되면:
  - 화면 내 모든 낙하물 제거
  - 플레이어에 이미 부착된 낙하물도 전부 제거
  - 제거된 낙하물 수를 추가 점수로 환산 지급
- 설계 의도:
  - 위기 상황 역전 수단 + 고점 운영 리스크/리턴 강화

### 12-6. 폭탄 점수 규칙 (확정)

- 기본 공식:
  - `폭탄 보너스 점수 = 제거된 낙하물 수 * bombClearScoreFactor`
- 제한 계수:
  - 별도 상한 계수 미적용 (화면 내 낙하물 총량이 자연 상한 역할)
- 전제:
  - 폭탄 밸런스는 등장 확률/등장 라운드로 제어

### 12-7. 상태/UI 변경점

- 상단/중앙에 `Round N` 표시
- 라운드 종료 시:
  - 일시정지 오버레이 + 퍽 2지 선택 UI
  - 선택 완료 후 다음 라운드 시작 카운트다운(선택)
- 인게임 HUD:
  - 현재 점수
  - 현재 라운드
  - 다음 라운드까지 남은 시간
  - 랭킹 1위 점수
  - 활성 퍽 요약(아이콘 또는 짧은 텍스트)

### 12-8. 데이터 구조 제안

- `RoundConfig[]`
  - 라운드별 낙하 속도, 생성 간격, 동시 생성 수
- `PerkDefinition[]`
  - id, 이름, 설명, 스택 가능 여부, 적용 범위(즉시/다음 라운드)
- `RunState`
  - 현재 라운드, 보유 퍽 목록, 다음 라운드 예약 효과, 라운드 페이즈 상태

### 12-9. v0.2 확정 필요 항목

- 라운드 종료 시간 기본값(`roundDurationSec`) 최종 수치
- 퍽 등장 가중치(공격/안정/유틸 계열 비율)
- 폭탄 등장 확률 기본값 및 라운드별 증가 여부
- 퍽별 최대 중첩 수치 테이블 확정

