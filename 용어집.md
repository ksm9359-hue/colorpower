# 용어집 (Glossary)

프로젝트의 MD 문서들에서 사용한 주요 용어를 정리한 기준 문서입니다.

---

## 1) 게임 기획/플레이 용어

- `장르`: 게임의 플레이 스타일 분류. 본 프로젝트는 캐주얼 회피/수집 장르를 지향한다.
- `핵심 재미`: 플레이어가 반복 플레이에서 느껴야 하는 중심 재미 요소.
- `플레이 목표`: 한 판에서 달성하려는 목적. 점수 누적, 생존, 매칭 성공 등이 포함된다.
- `플레이어`: 사용자가 직접 조작하는 게임 내 캐릭터(현재 회색 고정 오브젝트).
- `조작`: 입력 방식. 키보드(`A/D`, `Left/Right`)와 모바일 터치(슬라이드) 포함.
- `낙하물`: 화면 상단에서 생성되어 아래로 떨어지는 오브젝트.
- `컬러 타입`: 낙하물의 색상 분류(`R/B/G/Y/P`).
- `충돌`: 낙하물과 플레이어의 접촉 판정 이벤트.
- `부착`: 충돌한 낙하물이 플레이어 상태에 쌓이는 규칙.
- `부착 수`: 현재 플레이어에 쌓여 있는 낙하물 총합.
- `색상별 부착 카운트`: 색상 단위로 쌓인 부착 수.
- `3매칭`: 같은 색상 부착이 3개가 되어 제거 보너스를 발생시키는 규칙.
- `보너스 점수`: 특정 조건(예: 3매칭) 충족 시 추가 지급되는 점수.
- `게임오버`: 실패 상태. 현재는 부착 수가 한계를 초과하면 발생.
- `재시작`: 게임오버 후 새 판을 시작하는 동작(`Space` 입력).
- `학습 구간`: 초반 난이도 구간. 조작과 규칙을 익히는 구간.
- `선택 압박`: 중반 이후 피할지/받을지 판단이 어려워지는 상태.
- `리스크/리턴`: 위험을 감수할수록 높은 보상을 기대하는 구조.
- `즉사 리스크`: 작은 실수로 빠르게 게임오버될 가능성이 높은 상태.

---

## 2) 밸런스/수식 용어

- `밸런스`: 점수, 난이도, 속도, 스폰량 등의 수치 체계 전체.
- `오프셋 수치`: 실제 체감 난이도를 이동시키는 기준 수치값(상향/하향 기준).
- `Offset Source`: 밸런스 원본 기준 문서/파일을 뜻하는 표현.
- `밸런스 팩터`: 커브 형태를 결정하는 입력 파라미터 집합.
- `Factor Set`: 밸런스 팩터 묶음(현재 운영 파라미터 표).
- `Baseline`: 기준선. 비교/실험의 출발점이 되는 값 세트.
- `프리셋`: 특정 의도(학습 친화/하드코어)에 맞춘 팩터 조합.
- `라운드 기준 시간 (`roundDurationSec`)`: 커브 계산 시 0~1 정규화의 기준이 되는 총 시간.
- `정규화 시간 (`p`)`: `t / roundDurationSec`을 0~1 범위로 제한한 값.
- `clamp`: 값을 최소/최대 범위 안으로 고정하는 함수.
- `floor`: 소수점을 버려 정수로 내리는 함수.
- `round`: 값을 반올림하는 함수.
- `커브 지수 (power)`: 시간 경과에 따른 변화의 가속/완만 정도를 정하는 지수값.
- `스폰 간격 (`spawnDelay`)`: 낙하물 웨이브가 생성되는 시간 간격(ms).
- `스폰 간격 시작값 (`spawnDelayStartMs`)`: 게임 시작 시 스폰 간격.
- `스폰 간격 최소값 (`spawnDelayMinMs`)`: 더 이상 줄어들지 않는 하한값.
- `스폰 커브 지수 (`spawnCurvePower`)`: 스폰 간격 감소 곡선의 형태를 제어.
- `낙하 속도 (`dropSpeed`)`: 낙하물이 아래로 이동하는 속도.
- `낙하 속도 시작값 (`dropSpeedStart`)`: 게임 시작 시 낙하 속도.
- `낙하 속도 최대값 (`dropSpeedMax`)`: 속도 증가 상한값.
- `속도 커브 지수 (`speedCurvePower`)`: 낙하 속도 증가 곡선의 형태를 제어.
- `동시 스폰 수 (`spawnCount`)`: 한 번에 생성되는 낙하물 개수.
- `동시 스폰 최소/최대`: 동시 스폰 수의 하한/상한.
- `동시 스폰 커브 지수 (`spawnCountCurvePower`)`: 동시 스폰 증가 타이밍/가속을 제어.
- `점수 난이도 배율 (`scoreDifficultyFactor`)`: 난이도에 따라 기본 점수에 더해지는 오프셋 계수.
- `기본 점수 (`scorePerCatch`)`: 낙하물 1개 처리 시 기본 획득 점수.
- `3매칭 보너스 (`matchBonusScore`)`: 동일 색상 3매칭 성공 시 지급되는 점수.
- `난이도 점수 오프셋 (`difficultyScoreOffset`)`: 난이도에 따라 추가되는 점수량.
- `획득 점수 (`catchScore`)`: 실제 충돌 1회당 반영되는 점수.
- `샘플 포인트`: 특정 시간(`t`)에서 수식을 대입해 확인하는 대표 점.
- `스냅샷 (`snapshot.md`)`: 현재 팩터 기준의 계산 결과 요약 파일.
- `커브 그래프`: 시간 대비 수치 변화 라인을 시각화한 그래프(SVG).

---

## 3) 구현/엔진 용어

- `프로토타입`: 핵심 재미 검증을 목적으로 만든 초기 구현 버전.
- `엔진/런타임`: 게임을 실행하는 기술 기반(Phaser + Vite + TypeScript).
- `Phaser`: 2D 게임 구현용 JavaScript/TypeScript 프레임워크.
- `Scene`: Phaser에서 게임 상태/로직을 묶어 관리하는 단위.
- `스프라이트`: 화면에 렌더링되는 2D 이미지 오브젝트.
- `텍스처`: 스프라이트의 이미지 데이터 리소스.
- `이펙트`: 시각적 피드백(플래시, 파티클, 부유 텍스트 등).
- `파티클`: 작은 조각 오브젝트를 퍼뜨려 효과를 표현하는 연출 방식.
- `UI`: 점수/부착 수/상태 등 플레이 정보를 보여주는 화면 요소.
- `하이스코어`: 최고 점수 기록 데이터.
- `localStorage`: 브라우저에 소규모 데이터를 저장하는 클라이언트 저장소.

---

## 4) 개발/배포 용어

- `Node.js LTS`: 장기 지원 버전 Node.js 런타임.
- `npm`: Node 패키지 매니저. 의존성 설치와 스크립트 실행에 사용.
- `스크립트`: `package.json`의 명령어 등록 항목.
- `npm run dev`: 개발 서버 실행 명령.
- `npm run build`: 프로덕션 빌드 생성 명령.
- `npm run preview`: 빌드 결과 로컬 확인 명령.
- `npm run typecheck`: TypeScript 타입 검사 명령.
- `npm run balance:graphs`: 밸런스 그래프/스냅샷 생성 명령.
- `브랜치`: Git에서 변경 이력을 분기해 관리하는 작업 단위.
- `커밋`: Git 변경 이력의 저장 단위.
- `push`: 로컬 커밋을 원격 저장소로 업로드하는 동작.
- `GitHub Actions`: CI/CD 자동화 워크플로우 실행 환경.
- `워크플로우`: 자동 빌드/테스트/배포 절차를 정의한 설정 파일.
- `GitHub Pages`: 정적 웹사이트 호스팅 서비스.
- `Netlify`: 정적 사이트 배포/호스팅 플랫폼.
- `프로덕션 빌드`: 실제 배포용으로 최적화된 결과물.
- `트러블슈팅`: 문제 발생 시 원인 분석과 해결 절차.
- `MVP`: 최소 기능 제품. 핵심 가설 검증에 필요한 최소 범위 구현.

---

## 5) 문서 운영 용어

- `README`: 프로젝트 소개와 시작 방법을 담는 기본 문서.
- `CHANGELOG`: 버전별 주요 변경 이력 문서.
- `DEVLOG`: 날짜별 작업/이슈/다음 작업을 기록하는 개발 로그.
- `RETROSPECTIVE`: 잘한 점/아쉬운 점/배운 점을 정리하는 회고 문서.
- `SETUP`: 개발 환경 설치 및 실행 가이드 문서.
- `DEPLOY`: 배포 절차와 확인 방법 문서.
- `ASSET_CREDITS`: 사용 에셋의 출처와 라이선스를 기록하는 문서.
- `기획 이력`: 기획 변경 과정과 확정 항목을 누적 관리하는 기록.
